import { RecentTransactions } from "@/widgets/recent-transactions";

export default function TransactionsPage() {
  return (
    <div className="animate-fade-up">
      <RecentTransactions title="Транзакции" />
    </div>
  );
}
